# 2026-05-23

- Applied personal OpenClaw cost/reliability baseline:
  - kept OpenAI default on `gpt-5.4-mini`
  - removed `gpt-5.5` from automatic fallback path
  - made heartbeat inert via empty `HEARTBEAT.md`
  - added compaction safeguard and memory flush
  - added memory search using `text-embedding-3-small`
  - disabled dreaming and active-memory for baseline testing
  - capped subagent concurrency/depth
- Verification:
  - `HEARTBEAT.md` is effectively empty
  - gateway restart hit a scope-approval gate and stayed on the existing process
  - current warnings: active-memory config present while disabled; persisted plugin registry policy is stale
- Rollback: restore the timestamped backup of `~/.openclaw/openclaw.json` and restart gateway safely.
- Verify by checking effective default model, fallback list, heartbeat interval, active-memory, dreaming, compaction, memory search, Slack policies, and subagent limits after restart.
