# Memory

## Workspace

- The `Sylvgira/Bito` private repo was created and the workspace was pushed there.
- Sylvain wants recurring repo backup/sync automation implemented as an *OpenClaw cronjob*, not a macOS LaunchAgent or a local crontab entry.
- For automation work here, prefer the lightest model that can complete the task.
- The lead refinery should stay *opportunity-pattern-first*, not industry-first: qualify businesses by business strength, visible website weakness, offer fit, and contactability before AI review.
- The signal-led discovery model now centres on patterns such as hidden quality, weak quote flow, underused proof, premium service/low-trust presentation, weak service/search structure, and B2B RFQ friction.
- The CRM sheet presentation matters; Sylvain prefers the improved V1-style design/formatting over the flatter V2 look.

## GitHub / Bitwarden

- Bitwarden unlock worked locally in this workspace after setup issues were resolved.
- The GitHub credential hunt involved multiple items in the Bito vault; the usable PAT was the intended source for `gh` auth.
- A missing access key was later found in Bitwarden and added successfully; for future auth/access issues in this workspace, check Bitwarden before using fallback methods.
- Sylvain shared a new lead-engine direction spec: discovery should be opportunity-pattern-first, not industry-first. The main shift is to score business strength, website weakness, offer fit, and contactability before AI review, then generate an evidence pack for the stronger candidates. Existing `scripts/site-audit` already covers screenshot/Lighthouse/axe collection and can be reused as the audit substrate.
- The lead-engine skills were updated to match the pattern-first refinery model, including a canonical `lead-refinery.md` reference and a v2 sheet schema that surfaces pattern, scores, and model logging.
- `goplaces` authenticates by loading `BW_SESSION` from `scripts/bw-session`, then reading the `Google Places API Key` item from Bitwarden and exporting it as `GOOGLE_PLACES_API_KEY` for each CLI call.
- The first promising Melbourne candidate from the pattern-first pass is `Pro Pest Control Melbourne` / `melbourne-pestcontrol.com.au`. The site is live but generic, with a keyword-heavy title and a thin quote-flow wedge, so it looks more like a weak-quote-flow opportunity than a benchmark.
- The CRM now uses the `Lead Engine CRM` Google Sheet at `1g5JVNuS5-kqCLUpY1leD-MgB3Yo_kGRWBrfy9dk4X1Q`. The active tab is `Leads V2`, and it now includes an explicit `fit_bucket` column so every candidate can stay in the sheet for manual review while still being easy to sort.
- Added `LikelihoodOfRebuildScore` to the Lead Engine CRM as a separate readiness/receptiveness score from `website_weakness_score`. It uses component columns for `CommercialMotivation`, `TechnicalDebt`, `ExistingInvestmentSignals`, `BusinessActivitySignals`, and `FrictionOrRiskSignals`, plus bucket/rationale fields; Legend rows 23-30 document the rubric.
